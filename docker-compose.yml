services:
  tokenvue:
    build:
      context: .
      dockerfile: Dockerfile
    image: tokenvue:local
    init: true
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
    restart: unless-stopped
